# Microwatt Tests

To run these you need icarus verilog, and a ppc64le toolchain. 

On Fedora

```
sudo dnf install iverilog gcc-powerpc64le-linux-gnu
```

And on Ubuntu:

```
sudo apt install iverilog gcc-powerpc64le-linux-gnu
```


